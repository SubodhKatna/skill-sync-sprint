server.port=8888
spring.application.name=config-server

# Git backend — point to your config repo
spring.cloud.config.server.git.uri=${CONFIG_GIT_URI:https://github.com/your-org/skillsync-config}
spring.cloud.config.server.git.default-label=${CONFIG_GIT_BRANCH:main}
spring.cloud.config.server.git.clone-on-start=true
spring.cloud.config.server.git.force-pull=true

# Optional: private repo credentials
spring.cloud.config.server.git.username=${CONFIG_GIT_USERNAME:}
spring.cloud.config.server.git.password=${CONFIG_GIT_PASSWORD:}

# Eureka registration
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}

management.endpoints.web.exposure.include=health,info,refresh
management.endpoint.health.show-details=always
